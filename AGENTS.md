# Codex Agent Instructions

## Context7 Requirement

When working in this project, Codex agent **must use `context7` MCP** whenever task involves any third-party library, framework, SDK, API, or CLI tool used by repo.

Use `context7` to fetch current official documentation before implementing, changing, or debugging integration code that depends on external libraries.
